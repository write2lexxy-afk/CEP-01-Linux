# CEP-01 — Linux Service Management

## Overview

Linux services on Ubuntu are managed primarily through `systemd`.

The `systemctl` command is used to inspect and control services.

General syntax:

```bash
systemctl ACTION SERVICE
