# Sudo and Linux Privileges

## Purpose

This document records the Linux privilege and sudo configuration identified during the CEP-01 Linux baseline assessment.

The objective is to understand how Linux separates normal-user access from administrative privileges and how `sudo` is used to execute privileged commands.

---

## 1. Current User

The current Linux user is:

```bash
whoami
